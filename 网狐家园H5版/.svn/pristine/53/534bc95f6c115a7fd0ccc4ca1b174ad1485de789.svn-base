/**
 * 用于数据缓存
 * http请求缓存 
 */
namespace cache {
    export class localization {
        private _cache: {[key:string]: any} = {};
        constructor() {
            
        }

        /**
         * http请求缓存 JSON 不加密
         */
        public saveCache(key:string,value: any) {
            this._cache[key] = value;
        }

        public updateCache(key: string,value: any) {

        }

        public removeCacheByKey(key: string) {

        }

        public getCacheByKey(key: string) {
            return this._cache[key];
        }
    }
}